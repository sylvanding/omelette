## 2026-04-29 - US-003
- Implemented Docker health checks for both services
- Files changed: Dockerfile, frontend/Dockerfile, docker-compose.yml
- Backend: python urllib check to /api/v1/settings/health
- Frontend: wget check to localhost:80
- docker-compose.yml: frontend depends on backend condition: service_healthy

## 2026-04-29 20:47 - US-002
- Added PDFViewer tests with react-pdf mock (7 tests)
- Added CitationGraphView tests with d3 mocks (8 tests)
- Added NodeDetailPanel tests (11 tests)
- Added useTheme hook tests with matchMedia mocking (7 tests)
- Added useSidebar hook tests with localStorage persistence (8 tests)
- Fixed existing CitationCard et al. test (isExpanded was false)
- Fixed PDFViewer keyboard handler missing dependency (useCallback)
- Added aria-labels to PDFViewer toolbar and NodeDetailPanel close button
- Added aria-live regions to PDFViewer indicators
- Total tests: 76 passing (was 34)
- **Learnings for future iterations:**
  - react-pdf requires full mock to avoid import.meta.url crash in jsdom
  - d3 modules (force, selection, drag, zoom) must all be mocked for CitationGraphView
  - framer-motion AnimatePresence doesn't animate in jsdom - use isExpanded=true for state tests
  - Test utils export `render` from @testing-library/react separately
  - window.matchMedia not available in jsdom - must mock with Object.defineProperty
---

## 2026-04-29 20:55 - US-006
- Added aria-labels to remaining icon-only buttons
- Files changed: MobileBottomNav.tsx (More button + nav element), ProjectDetail.tsx (back button), PDFReaderLayout.tsx (back button)
- CitationCard, PapersPage, NodeDetailPanel, CitationGraphDialog already had aria-labels from previous work
- All 76 tests passing, lint and typecheck clean
- **Learnings for future iterations:**
  - Most aria-labels were already in place from US-005 work
  - When adding aria-labels, use t() for i18n support with fallback strings
  - The `<nav>` element should also have `aria-label` for landmark navigation
---
