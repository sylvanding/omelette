# Ralph Progress Log
Started: Thu Apr 30 07:12:46 AM CST 2026
---

## Codebase Patterns
- Backend uses `ApiResponse` envelope for all endpoints with `{code, message, data}` structure
- LLM calls use `llm.chat_json()` for JSON responses and `task_type` for mock routing
- New services go in `app/services/` with constructor taking `LLMClient`
- Each route module exports `router = APIRouter(tags=["..."])` — mounted via `__init__.py`
- New route modules need BOTH an import AND `api_router.include_router()` call in `__init__.py`
- Router prefix is set in `include_router()` call in `__init__.py`, not on the `APIRouter()` constructor
- Frontend API calls use axios instance from `@/lib/api` that unwraps `response.data`
- Mock responses live in `app/services/llm/adapters/mock_adapter.py` — add task_type entries
- Test fixtures use `client` (AsyncClient), `project_id`, and `setup_db` from conftest.py
- Frontend uses `@tanstack/react-query` for data fetching with `useQuery`
- shadcn/ui components may not exist for everything — use native HTML elements when missing (e.g., `<input type="range">` instead of missing Slider)
- Endpoint tests that query DB should use `app.dependency_overrides[get_db]` for mock session injection
- Use `func` from `sqlalchemy` for aggregate functions, NOT `db.func` (AsyncSession has no `func` attribute)
- httpx AsyncClient DELETE doesn't support `json` or `content` params — use `client.request("DELETE", ..., content=...)` for DELETE with body
- Pre-commit hooks (ruff, ruff-format) modify files in-place — always stage after first commit attempt

---

## [2026-04-30 07:20] - FEAT-001
- Implemented augmented PDF reading with skimming highlights and inline citation cards
- Backend: Created `augmented_reading_service.py` with 3 methods (highlights, citation cards, definitions)
- Backend: Added 3 endpoints to papers.py: POST /highlights, GET /citation-cards, GET /definitions
- Backend: Added Pydantic schemas in `schemas/augmented_reading.py`
- Backend: Added mock responses for all 3 task types
- Backend: Added 6 tests (success + 404 for each endpoint)
- Frontend: Added `augmentedReadingApi` to services/api.ts with typed interfaces
- Frontend: Created HighlightOverlay component with category toggles and opacity control
- Frontend: Created CitationCardPanel component with citations/definitions tabs
- Frontend: Integrated as new "Highlights" and "Citations" tabs in PDFReaderLayout
- Files changed:
  - backend/app/services/augmented_reading_service.py (new)
  - backend/app/schemas/augmented_reading.py (new)
  - backend/app/api/v1/papers.py (modified)
  - backend/app/services/llm/adapters/mock_adapter.py (modified)
  - backend/tests/test_api_projects_papers.py (modified)
  - frontend/src/services/api.ts (modified)
  - frontend/src/components/pdf-reader/PDFReaderLayout.tsx (modified)
  - frontend/src/components/pdf-reader/HighlightOverlay.tsx (new)
  - frontend/src/components/pdf-reader/CitationCardPanel.tsx (new)
- **Learnings for future iterations:**
  - When adding new LLM task types, always add corresponding mock responses to avoid test failures
  - The `get_or_404` helper validates paper existence — no need to use the returned value if not needed
  - Frontend Slider component wasn't available in shadcn/ui — used native `<input type="range">` instead
  - Always check for component existence before importing from `@/components/ui/`

---

## [2026-04-30 07:45] - FEAT-002
- Implemented evidence consensus visualization for research questions
- Backend: Created `evidence_consensus_service.py` with stance classification (support/contradict/mixed)
- Backend: Added POST /projects/{id}/rag/evidence-consensus endpoint in rag.py with Pydantic schemas
- Backend: Added mock LLM response for `evidence_consensus` task type
- Backend: Added 5 tests covering structured data, findings, confidence bounds, percentage sums, empty retrieval
- Frontend: Created EvidenceConsensus component with recharts bar chart, paper findings, and stance badges
- Frontend: Added typed API integration in services/api.ts
- Files changed:
  - backend/app/services/evidence_consensus_service.py (new)
  - backend/app/api/v1/rag.py (modified)
  - backend/app/services/llm/adapters/mock_adapter.py (modified)
  - backend/tests/test_evidence_consensus.py (new)
  - frontend/src/services/api.ts (modified)
  - frontend/src/components/evidence/EvidenceConsensus.tsx (new)
- **Learnings for future iterations:**
  - RAG endpoints that depend on ChromaDB retrieval need mocked `retrieve_only` in tests
  - Pre-commit hooks (ruff, ruff-format) may reformat newly created Python files — re-stage after first commit attempt
  - Percentage rounding across 3 categories can sum to 99.9 — allow 1% tolerance in tests
  - Recharts is already available in the project for charting needs
  - The rag.py file uses inline Pydantic schemas (not separate schema files) for request/response models
---

## [2026-04-30 08:00] - FEAT-003
- Implemented multi-document contradiction detection
- Backend: Created contradiction_service.py with detect_contradictions() method that uses LLM to identify conflicting claims across papers
- Backend: Created analysis.py router with POST /projects/{id}/analysis/contradictions endpoint
- Backend: Registered analysis router in __init__.py with prefix /projects/{project_id}/analysis
- Backend: Added mock LLM response for contradiction_detection task type
- Backend: Added 8 tests (6 unit tests for service edge cases + 2 API endpoint tests)
- Frontend: Created ContradictionReport component with summary cards, topic filter badges, and contradiction detail cards
- Frontend: Added typed API integration (ContradictionPair, ContradictionResult, contradictionsApi) to services/api.ts
- Files changed:
  - backend/app/services/contradiction_service.py (new)
  - backend/app/api/v1/analysis.py (new)
  - backend/app/api/v1/__init__.py (modified)
  - backend/app/services/llm/adapters/mock_adapter.py (modified)
  - backend/tests/test_contradiction_detection.py (new)
  - frontend/src/components/contradiction/ContradictionReport.tsx (new)
  - frontend/src/services/api.ts (modified)
- **Learnings for future iterations:**
  - New route modules should be registered in __init__.py with both an import and include_router call with appropriate prefix
  - Tests for endpoints that query the DB should use app.dependency_overrides for get_db to inject mock sessions
  - The LLM prompt should instruct the model to return both contradiction pairs AND topic groupings in the same response
  - When adding task types to mock_adapter.py, ensure the JSON structure matches exactly what the service expects
---

## [2026-04-30 08:20] - FEAT-004
- Implemented iterative visual discovery with prior/derivative/similarity graph modes
- Backend: Added `mode` parameter to `get_citation_graph()` supporting "all", "prior", "derivative", "similarity"
- Backend: Added `_get_similarity_graph()` method that computes cosine similarity from ChromaDB embeddings
- Backend: Prior Works mode returns only references (type: "cites"), Derivative Works only citations (type: "cited_by")
- Backend: Added 5 new tests covering all modes and API mode parameter
- Frontend: Added mode toggle buttons (All, Prior Works, Derivative, Similarity) with icons
- Frontend: Updated CitationGraphDialog with mode state and onNodeClick for iterative seed switching
- Frontend: Edge types now visually distinct (cites=blue, cited_by=orange, similar=purple with thickness)
- Files changed:
  - backend/app/services/citation_graph_service.py (modified)
  - backend/app/api/v1/papers.py (modified)
  - backend/tests/test_citation_graph.py (modified)
  - frontend/src/components/citation-graph/CitationGraphView.tsx (modified)
  - frontend/src/components/citation-graph/CitationGraphDialog.tsx (modified)
  - frontend/src/services/api.ts (modified)
  - frontend/src/lib/query-keys.ts (modified)
- **Learnings for future iterations:**
  - Edge types now use "cites" (seed→ref), "cited_by" (cite→seed), "similar" (embedding match) — must be consistent across service and frontend
  - Similarity graph uses paper_id as node ID (not S2 ID) since similarity is computed within project — frontend handles both ID formats
  - When ChromaDB is unavailable, similarity mode returns empty graph gracefully rather than erroring
  - Query keys must include mode parameter to avoid stale cache when switching between modes
---

## [2026-04-30 08:45] - FEAT-005
- Implemented paper collections and smart tagging within projects
- Backend: Created Collection and CollectionPaper models with proper relationships and indexes
- Backend: Created collection_service.py with AI smart tagging via LLM (suggest_tags method)
- Backend: Added collections.py router with full CRUD + paper association + tag suggestion endpoints
- Backend: Added collection_id filter to existing papers list endpoint
- Backend: Registered collections router in __init__.py with prefix /projects/{project_id}/collections
- Backend: Added mock LLM response for smart_tagging task type
- Backend: Added 17 tests (4 service unit tests + 9 collection API tests + 2 smart tagging tests + 2 paper filter tests)
- Backend: Created Alembic migration for collections and collection_papers tables
- Frontend: Added Collection, CollectionPaper, CollectionDetail, PaperTagSuggestion types
- Frontend: Added collectionsApi with list/create/update/delete/getDetail/addPapers/removePapers/suggestTags
- Frontend: Created CollectionSidebar component with create/rename/delete/inline-edit and AI tag suggestion button
- Files changed:
  - backend/app/models/collection.py (new)
  - backend/app/models/__init__.py (modified)
  - backend/alembic/versions/a2b3c4d5e6f7_add_collections_tables.py (new)
  - backend/app/services/collection_service.py (new)
  - backend/app/api/v1/collections.py (new)
  - backend/app/api/v1/__init__.py (modified)
  - backend/app/api/v1/papers.py (modified)
  - backend/app/services/llm/adapters/mock_adapter.py (modified)
  - backend/tests/test_collections.py (new)
  - frontend/src/types/index.ts (modified)
  - frontend/src/services/api.ts (modified)
  - frontend/src/components/collections/CollectionSidebar.tsx (new)
- **Learnings for future iterations:**
  - Use `func` imported from `sqlalchemy`, NOT `db.func` — AsyncSession doesn't expose `func`
  - httpx AsyncClient DELETE doesn't accept `json` param — use `client.request("DELETE", url, content=..., headers=...)`
  - Pre-commit hooks run ruff and ruff-format which modify files — need to re-stage after first commit failure
  - CollectionPaper junction table uses separate model (not SQLAlchemy association proxy) for easier CRUD operations
  - When filtering papers by collection, need to JOIN CollectionPaper on both the base query AND count query
- Avoid MySQL-specific types like LONGTEXT in models when tests run on SQLite — use Text instead
- The extraction endpoint POST /reviews/{id}/extract runs LLM for each paper, so tests need mock LLM responses
---

## [2026-04-30 09:10] - FEAT-006
- Implemented systematic review workflow with data extraction tables
- Backend: Created Review and ReviewExtraction models with JSON-stored columns and paper_ids
- Backend: Created review_service.py with LLM-based extraction (per-paper) and CSV export
- Backend: Added reviews.py router with full CRUD + extract + extractions + export endpoints
- Backend: Export endpoint uses StreamingResponse for CSV download
- Backend: Added mock LLM response for review_extraction task type
- Backend: Created Alembic migration for reviews and review_extractions tables
- Backend: Added 13 tests (4 service unit + 6 review API + 3 extraction API)
- Frontend: Added Review, ReviewColumn, ExtractionResult, ExtractionProgress types
- Frontend: Added reviewsApi with list/create/update/delete/extract/getExtractions methods
- Frontend: Created ReviewsPage with column builder, extraction table, and CSV export
- Frontend: Added /reviews route and navigation link in ProjectDetail sidebar
- Files changed:
  - backend/app/models/review.py (new)
  - backend/app/models/__init__.py (modified)
  - backend/alembic/versions/f0a1b2c3d4e5_add_reviews_tables.py (new)
  - backend/app/services/review_service.py (new)
  - backend/app/api/v1/reviews.py (new)
  - backend/app/api/v1/__init__.py (modified)
  - backend/app/services/llm/adapters/mock_adapter.py (modified)
  - backend/tests/test_reviews.py (new)
  - frontend/src/types/index.ts (modified)
  - frontend/src/services/api.ts (modified)
  - frontend/src/pages/project/ReviewsPage.tsx (new)
  - frontend/src/App.tsx (modified)
  - frontend/src/pages/ProjectDetail.tsx (modified)
- **Learnings for future iterations:**
  - MySQL-specific types like LONGTEXT fail on SQLite test databases — use Text instead
  - Export endpoints return StreamingResponse, not ApiResponse envelope
  - The extract endpoint processes papers one-by-one (no SSE streaming implemented yet)
  - Frontend uses native table for extraction display rather than DataTable component
  - useTranslation import creates unused variable warnings if `t` isn't actually used

---

## [2026-04-30 09:30] - FEAT-007
- Implemented concept knowledge graph and topic pages
- Backend: Created concept_service.py with extract_concepts(), build_concept_graph(), and get_topic_page() methods
- Backend: Added concepts.py router with POST /extract, GET /graph, GET /{name}/page endpoints
- Backend: Registered concepts router in __init__.py with prefix /projects/{project_id}/concepts
- Backend: Added mock LLM responses for concept_extraction, concept_graph_building, topic_page_generation
- Backend: Added 10 tests (6 service unit + 4 API endpoint tests)
- Frontend: Created ConceptsPage with searchable concept card grid, relationship display, and clickable topic page viewer
- Frontend: Added ConceptNode, ConceptEdge, TopicPage types to services/api.ts and types/index.ts
- Frontend: Added /concepts route and "Concepts" navigation link (Network icon) in ProjectDetail sidebar
- Files changed:
  - backend/app/services/concept_service.py (new)
  - backend/app/api/v1/concepts.py (new)
  - backend/app/api/v1/__init__.py (modified)
  - backend/app/services/llm/adapters/mock_adapter.py (modified)
  - backend/tests/test_concepts.py (new)
  - frontend/src/services/api.ts (modified)
  - frontend/src/types/index.ts (modified)
  - frontend/src/pages/project/ConceptsPage.tsx (new)
  - frontend/src/App.tsx (modified)
  - frontend/src/pages/ProjectDetail.tsx (modified)
- **Learnings for future iterations:**
  - Concept extraction limits to 30 papers and 1500 chars per abstract to avoid token overflow
  - Topic page generation filters related papers by paper_id match from concept extraction
  - Frontend concept cards use native button elements with onClick instead of Link for topic page navigation
  - The same extract+build pipeline is reused for both POST /extract and GET /graph endpoints (no caching yet)

---

## [2026-04-30 10:00] - FEAT-008
- Implemented AI library organization with auto-tagging and metadata repair
- Backend: Created library_service.py with check_health(), repair_metadata(), suggest_tags(), and cluster_papers() methods
- Backend: Added library.py router with GET /health, POST /repair, POST /auto-tag, POST /clusters endpoints
- Backend: Health check scans papers for missing abstract, authors, year, journal, doi, citation_count fields
- Backend: Repair queries Semantic Scholar API by DOI first, then falls back to title search
- Backend: Auto-tag uses LLM to suggest 3-5 descriptive tags per paper
- Backend: Clustering uses LLM to group papers into 3-7 thematic clusters
- Backend: Registered library router in __init__.py with prefix /projects/{project_id}/library
- Backend: Added mock LLM responses for library_auto_tag, library_cluster_analysis task types
- Backend: Added 17 tests (10 service unit + 7 API endpoint tests)
- Frontend: Added LibraryHealth, RepairResult, AutoTagResult, ClusterResult types
- Frontend: Added libraryApi with health/repair/autoTag/clusters methods
- Frontend: Created LibraryPage with tabbed interface (Health Check, Repair, Auto-Tag, Cluster)
- Frontend: Added /library route and "Library" navigation link (FolderOpen icon) in ProjectDetail sidebar
- Files changed:
  - backend/app/services/library_service.py (new)
  - backend/app/api/v1/library.py (new)
  - backend/app/api/v1/__init__.py (modified)
  - backend/app/services/llm/adapters/mock_adapter.py (modified)
  - backend/tests/test_library.py (new)
  - frontend/src/types/index.ts (modified)
  - frontend/src/services/api.ts (modified)
  - frontend/src/pages/project/LibraryPage.tsx (new)
  - frontend/src/App.tsx (modified)
  - frontend/src/pages/ProjectDetail.tsx (modified)
- **Learnings for future iterations:**
  - ESLint catches unused imports/variables — check imports before committing
  - The repair_metadata endpoint identifies papers needing repair automatically (no body needed) — it scans for null/empty fields
  - Semantic Scholar lookup by DOI is more reliable than title search — try DOI first, fall back to title
  - LibraryService.check_health() is synchronous (no LLM needed) — pass None for llm parameter
  - httpx.AsyncClient proxy uses settings.http_proxy via _get_proxy() helper for consistency with search_service
  - Tab-based UI pattern: use state for activeTab, conditionally render sections, each tab has its own mutation
- Route ordering matters in FastAPI: static paths (e.g., `/analytics`) MUST be defined before parameterized paths (e.g., `/{paper_id}`) in the same file to avoid route conflicts
- Reading time is tracked via `useReadingTimer` hook in `frontend/src/hooks/useReadingTimer.ts` — auto-flushes on tab hide and component unmount

---

## [2026-04-30 10:30] - FEAT-009
- Implemented enhanced reading analytics with productivity metrics and knowledge gap analysis
- Backend: Created `analytics_service.py` with compute_papers_per_week, compute_avg_read_time, compute_reading_streak, compute_domain_coverage, compute_citation_impact, analyze_knowledge_gaps methods
- Backend: Added `POST /projects/{id}/papers/reading-sessions` endpoint in papers.py (moved before /{paper_id} routes)
- Backend: Enhanced `GET /projects/{id}/papers/analytics` with new metrics (papers_per_week, avg_read_time_seconds, reading_streak_days, domain_coverage, citation_impact)
- Backend: Created `analytics.py` router with `GET /projects/{id}/analytics/knowledge-gaps` endpoint
- Backend: Created ReadingSession model and Alembic migration for reading_sessions table
- Backend: Added 14 tests (4 reading session API + 4 analytics API + 2 knowledge gap + 4 service unit tests)
- Frontend: Created `useReadingTimer` hook with auto-flush on visibility change and unmount
- Frontend: Integrated reading timer display in PDFReaderLayout header (Clock icon + formatted time)
- Frontend: Enhanced AnalyticsPage with productivity metric cards, citation impact bar chart, knowledge gaps list
- Frontend: Added ReadingSessionInput, KnowledgeGapItem, KnowledgeGapAnalysis types to api.ts
- Frontend: Added readingSessionApi and knowledgeGapsApi service objects
- Files changed:
  - backend/app/models/reading_session.py (new)
  - backend/app/models/__init__.py (modified)
  - backend/app/schemas/analytics.py (new)
  - backend/app/services/analytics_service.py (new)
  - backend/app/api/v1/analytics.py (new)
  - backend/app/api/v1/__init__.py (modified)
  - backend/app/api/v1/papers.py (modified)
  - backend/alembic/versions/a1b2c3d4e5f6_add_reading_sessions_table.py (new)
  - backend/tests/test_reading_analytics.py (new)
  - frontend/src/services/api.ts (modified)
  - frontend/src/lib/query-keys.ts (modified)
  - frontend/src/hooks/useReadingTimer.ts (new)
  - frontend/src/components/pdf-reader/PDFReaderLayout.tsx (modified)
  - frontend/src/pages/project/AnalyticsPage.tsx (modified)
  - frontend/src/i18n/locales/en.json (modified)
  - frontend/src/i18n/locales/zh.json (modified)
  - scripts/ralph/prd.json (modified)
- **Learnings for future iterations:**
  - FastAPI route ordering: static paths MUST come before parameterized paths in the same file, or they'll match incorrectly
  - ReadingSession model uses separate table (not Paper columns) for per-session granularity
  - The `formatReadingTime` utility function is in the same file as `useReadingTimer` hook for easy reuse
  - Knowledge gaps analysis uses journal distribution as proxy for topic diversity (no Concept model exists)
- Route path `/{paper_id}/feedback` inside `/projects/{project_id}/feed` prefix resolves to `/projects/{project_id}/feed/{paper_id}/feedback` — must use named subpath (not bare `/`) to avoid trailing-slash 307 redirects

---

## [2026-04-30 11:00] - FEAT-010
- Implemented personalized research feed with paper recommendations
- Backend: Created `feed_service.py` with `get_feed()` (LLM-based recommendations) and `submit_feedback()` methods
- Backend: Added feed.py router with GET /recommendations, POST /refresh, POST /{paper_id}/feedback endpoints
- Backend: Registered feed router in `__init__.py` with prefix `/projects/{project_id}/feed`
- Backend: Added mock LLM response for `research_feed` task type
- Backend: Added 13 tests (6 service unit + 7 API endpoint tests)
- Frontend: Created FeedPage with recommendation cards, expandable abstracts, and like/dislike feedback
- Frontend: Added feedApi to services/api.ts and FeedRecommendation/FeedResponse types to types/index.ts
- Frontend: Added /feed route and "Feed" navigation link (Sparkles icon) in ProjectDetail sidebar
- Files changed:
  - backend/app/services/feed_service.py (new)
  - backend/app/api/v1/feed.py (new)
  - backend/app/api/v1/__init__.py (modified)
  - backend/app/services/llm/adapters/mock_adapter.py (modified)
  - backend/tests/test_feed.py (new)
  - frontend/src/services/api.ts (modified)
  - frontend/src/types/index.ts (modified)
  - frontend/src/pages/project/FeedPage.tsx (new)
  - frontend/src/App.tsx (modified)
  - frontend/src/pages/ProjectDetail.tsx (modified)
- **Learnings for future iterations:**
  - Avoid bare `/` as route path inside a prefix — use named paths like `/recommendations` to prevent trailing-slash 307 redirects
  - `get_llm_client()` (not `get_llm`) is the correct import from `app.services.llm.client`
  - `get_db` is imported from `app.api.deps`, not `app.database`
  - Endpoint tests using `app.dependency_overrides[get_db]` with generator functions (`yield mock_session`) work reliably for mocking DB access
  - The feed service builds a researcher profile from library papers, reading history, liked papers, and keywords, then asks the LLM for recommendations
---
